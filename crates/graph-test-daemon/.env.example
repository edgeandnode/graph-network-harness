# Graph Stack Runner configuration
# Copy to .env and adjust paths as needed

# Path to graph-node repository (required)
GRAPH_NODE_SRC=/path/to/graph-node

# Logging level (optional, defaults to info for tracing)
RUST_LOG=warn
